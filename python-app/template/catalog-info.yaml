apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.app_name }}
  description: Python Flask API with Docker deployment and Jenkins CI/CD
  annotations:
    github.com/project-slug: febinqburst/${{ values.app_name }}
    backstage.io/techdocs-ref: dir:.
    jenkins.io/job-full-name: ${{ values.app_name }}-pipeline
    grafana/dashboard-url: https://grafana-qortexone.qburst.build/
spec:
  type: website
  owner: devops
  lifecycle: experimental
